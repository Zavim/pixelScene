import { MutableListAPI } from "@hmans/use-mutable-list";
import * as PP from "postprocessing";
import React, { ReactNode } from "react";
export declare const EffectPassContext: React.Context<MutableListAPI<PP.Effect>>;
export declare type EffectPassProps = {
    children?: ReactNode;
};
export declare const EffectPass: ({ children }: EffectPassProps) => JSX.Element;
