/// <reference types="react" />
import * as R3F from "@react-three/fiber";
export declare type CanvasProps = R3F.Props & {
    /** When true, enables React's StrictMode. */
    strict?: boolean;
};
export declare const Canvas: ({ strict, children, ...props }: CanvasProps) => JSX.Element;
