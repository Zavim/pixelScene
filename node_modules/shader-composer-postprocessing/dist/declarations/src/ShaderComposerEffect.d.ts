import * as PP from "postprocessing";
import { WebGLRenderer, WebGLRenderTarget } from "three";
import { PostProcessingEffectMaster } from "./PostProcessingEffectMaster";
export declare type ShaderComposerEffectProps = {
    root: ReturnType<typeof PostProcessingEffectMaster>;
    blendFunction?: PP.BlendFunction;
};
export declare class ShaderComposerEffect extends PP.Effect {
    private _shaderMeta;
    constructor({ root, blendFunction }: ShaderComposerEffectProps);
    update(renderer: WebGLRenderer, inputBuffer: WebGLRenderTarget, deltaTime: number): void;
}
